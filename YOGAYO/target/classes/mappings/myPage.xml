<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="UserDao">
  
  	<select id="myPage" resultType="int">
  	
  		<![CDATA[ 
  		SELECT TRUNCATE((SELECT 
  			AVG(WEIGHT) 
  		FROM 
  			FEED 
  		WHERE 
  			UMUM=#{uNum}
  		AND	
  			(LAST_DAY(NOW()) >  REGDATE 
  		AND 
  			LAST_DAY(NOW() - INTERVAL 1 MONTH) + INTERVAL 1 DAY < REGDATE)),0)
  		 ]]>
  	
  	</select>
  	
  	<select id="review" resultType="UserVo">
  		SELECT
  			FNUM, SLEVEL, CONTEXT, UIMG1, REGDATE
  		FROM
  			FEED
  		WHERE
  			UNUM=#{uNum}
  	</select>
  
  	
  </mapper>