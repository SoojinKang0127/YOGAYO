<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AdminDAO">

	<select id="getAllComment" resultType="AdminVo">
		SELECT c.CMTNUM, c.REGDATE, u.IMG, c.UNUM ,u.NAME, c.CRSNUM, c.RATE, c.PARENT, c.context
		FROM COMMENT c, USER u
		WHERE c.UNUM = u.UNUM order by REGDATE desc;
	</select>

	<delete id="deleteComment">
		DELETE FROM COMMENT WHERE CMTNUM = #{cmtNum} OR PARENT = #{parent};
	</delete>
</mapper>