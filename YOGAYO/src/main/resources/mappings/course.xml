<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CourseDao">

	<insert id="makeCourse" parameterType="CourseVo">
	INSERT INTO COURSE(TITLE,MNUM,DSCRT,MATERIAL,IMGPATH,SEQ1,SEQ2,SEQ3,SEQ4,SEQ5,SEQ6,SEQ7,SEQ8)
	values(#{title},#{uNum},#{dscrt},#{material},#{imgPath},#{seq1},#{seq2},#{seq3},#{seq4},#{seq5},#{seq6},#{seq7},#{seq8});
	</insert>
	
	<select id="selectAll" resultType="CourseVo">
		<!-- 테이블명 대소문자 구분 -->
		SELECT * FROM COURSE
	</select>
	
	
	<select id="selectOne" resultType="CourseVo">
		<!-- 테이블명 대소문자 구분 -->
		SELECT * FROM COURSE WHERE CRSNUM=#{crsNum}
	</select>
	
	<select id="selectPose" resultType="PoseVo">
	SELECT  P.TITLE, P.TIME FROM COURSE C, POSE P WHERE C.SEQ1=P.PNUM;
	</select>
	
	
</mapper>
